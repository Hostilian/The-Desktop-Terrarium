<Window x:Class="Terrarium.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Desktop Terrarium" 
        Height="200" 
        Width="1920"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseDown="Window_MouseDown"
        MouseMove="Window_MouseMove"
        Loaded="Window_Loaded">
    
    <Grid>
        <!-- Semi-transparent background for development (can be removed for full transparency) -->
        <Rectangle x:Name="BackgroundRect" Fill="#20000000" />
        
        <!-- Canvas for rendering entities -->
        <Canvas x:Name="RenderCanvas" 
                ClipToBounds="True"
                Background="Transparent">
        </Canvas>
        
        <!-- Status display (can be toggled) -->
        <StackPanel x:Name="StatusPanel" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Top" 
                    Margin="10"
                    Background="#AA000000">
            <TextBlock x:Name="FpsText" Foreground="White" Padding="5" FontSize="10"/>
            <TextBlock x:Name="EntityCountText" Foreground="White" Padding="5" FontSize="10"/>
            <TextBlock x:Name="EcosystemHealthText" Foreground="White" Padding="5" FontSize="10"/>
            <TextBlock x:Name="WeatherText" Foreground="White" Padding="5" FontSize="10"/>
        </StackPanel>
    </Grid>
</Window>
